{
  "mcpServers": {
    "pr-github": {
      "command": "${CLAUDE_PROJECT_DIR:-.}/mcp-server/venv/bin/python3",
      "args": ["${CLAUDE_PROJECT_DIR:-.}/mcp-server/server.py"],
      "env": {
        "GH_PAT": "${GH_PAT}"
      }
    }
  }
}
